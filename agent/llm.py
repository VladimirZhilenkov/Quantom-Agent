"""Anthropic SDK — tool_use call/response cycle."""


def call_llm(messages: list[dict], tools: list[dict]) -> dict:
    """Send messages to Claude with tool definitions and return response."""
    raise NotImplementedError
